{
  "compilerOptions": {
    "baseUrl": "./src", // Base directory for resolving modules
    "paths": {
      "@/*": ["*"] // Map `@/` to all files under `src`
    }
  },
  "include": ["src/**/*"], // Include all files in `src`
  "exclude": ["node_modules", "dist"] // Exclude generated files
}
